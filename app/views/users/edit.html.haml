%h3 Edit User Profile
-if @user.errors.any?
  .alert.alert-danger
    %a.close{"data-dismiss" => "alert"} ×
    =@user.errors.first[1].to_s.capitalize

=simple_form_for @user do |f|
  =f.input :name,  input_html: {class: 'input-lg' }
  =f.input :gender, collection: %w(Male Female), prompt: 'Select gender'
  =f.input :email
  =f.input :phone, as: :tel
  =f.input :address1, label: 'Address Line 1'
  =f.input :address2, label: 'Address Line 2'
  =f.input :city
  =f.input :state, collection: us_states, prompt: 'Select State'
  =f.input :zip_code
  =f.submit "Save", class: 'btn btn-default btn-primary btn-lg', data: {disable_with: "Saving..."}
  =link_to 'Cancel', user_path, class: 'btn btn-link'